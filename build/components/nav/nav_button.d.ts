import React, { ReactNode } from "react";
export interface AppNavButtonProps {
    className: string;
    children: ReactNode;
}
declare const AppNavButton: React.FC<AppNavButtonProps>;
export default AppNavButton;
