import React, { ReactNode } from 'react';
export interface LayoutFormProps {
    className?: string;
    children: ReactNode;
}
declare const LayoutForm: React.FC<LayoutFormProps>;
export default LayoutForm;
